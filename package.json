{
  "name": "prestoworld",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev:dashboard": "cd resources/presto/dashboard && vite",
    "build:dashboard": "cd resources/presto/dashboard && vite build",
    "preview:dashboard": "cd resources/presto/dashboard && vite preview"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  }
}